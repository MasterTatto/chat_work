import {chatID1} from "../chats/chatsReducer";
import {v4 as uuidv4} from "uuid";

const initialState = []


export const chatMessage = (state = initialState, action) => {
    switch (action.type) {


        default:
            return state
    }
}